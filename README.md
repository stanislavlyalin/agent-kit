Репозиторий содержит персональные переиспользуемые навыки Codex.
Скопируйте нужный каталог из `skills/` в `$CODEX_HOME/skills`; если `CODEX_HOME` не задан, используется `$HOME/.codex`.
Пример: `cp -R skills/review-guide "${CODEX_HOME:-$HOME/.codex}/skills/"`.
